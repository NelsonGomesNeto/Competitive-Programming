#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

int main()
{
  ldouble x; scanf("%Lf", &x);
  printf("%d\n", (int) floor(log10(x)));
  return 0;
}