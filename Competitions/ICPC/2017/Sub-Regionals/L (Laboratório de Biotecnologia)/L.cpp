#include <bits/stdc++.h>
int all[(int) 1e6]

int fft()
{
  if ()
}

int main()
{
  memset(all, 0, sizeof(all));
  char s[(int) 1e6]; scanf("%s", s);
  int size = strlen(s);
  int arr[size];
  for (int i = 0; i < size; i ++)
    arr[i] = s[i] - 'a' + 1;

  fft(0, size);
  printf("%d\n", possible);
  return(0);
}