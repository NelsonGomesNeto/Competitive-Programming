#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

int main()
{
  int n; scanf("%d", &n);
  while (n --)
  {
    lli x, y; scanf("%lld %lld", &x, &y);
    lli ans = 1e10, now = x;
    for (int t = 0; t <= 86399)
    {

      ans = min
    }
  }
  return 0;
}