#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

const string hello = "Hello SECOMP 2021!";

int main()
{
  int n;
  while (~scanf("%d", &n))
  {
    for (int i = 0; i < n; i++)
      printf("%s\n", hello.c_str());
  }
  return 0;
}