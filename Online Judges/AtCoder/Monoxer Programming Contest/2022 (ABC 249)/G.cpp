#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

const int maxN = 1e3; int n; lli k;
lli a[maxN], b[maxN];

int main()
{
  while (~scanf("%d %lld", &n, &k))
  {
    for (int i = 0; i < n; ++i)
      scanf("%lld %lld", &a[i], &b[i]);

    for (int bit = 30 - 1; bit >= 0; --bit)
    {

    }
  }
  return 0;
}