#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

int main()
{
  int n, m;
  scanf("%d %d", &n, &m);
  printf("%s\n", m >= n ? "Yes" : "No");
  return 0;
}