#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

int main()
{
  int x;
  while (scanf("%d", &x) != EOF)
  {
    printf("%s\n", x >= 30 ? "Yes" : "No");
  }
  return 0;
}