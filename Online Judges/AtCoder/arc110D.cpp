#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

/*
        b[i]!
----------------------
a[i]! * (b[i] - a[i])!

sequencesPossible:
sp = product choose(M, a[i]) for all i

ans = inv(fat(a[i])) * sp;

for all i:
  ans +=

*/

int main()
{
  
  return 0;
}