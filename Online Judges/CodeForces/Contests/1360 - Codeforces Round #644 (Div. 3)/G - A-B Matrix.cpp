#include <bits/stdc++.h>
#define DEBUG if(0)
#define lli long long int
#define ldouble long double
using namespace std;

const int maxN = 50; int n, m, a, b;

// source (0) -[a]> rows -[inf]> columns -[b]> sink
const int maxVertices;

int main()
{
  int t; scanf("%d", &t);
  for (int tt = 1; tt <= t; tt++)
  {
    scanf("%d %d %d %d", &n, &m, &a, &b);
  }
  return 0;
}