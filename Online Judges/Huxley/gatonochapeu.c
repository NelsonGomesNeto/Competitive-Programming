#include <stdio.h>
#include <stdbool.h>
#include <math.h>

long long int altIni, workers, n;

bool checkLog()
{
  long long int backAlt = altIni, pot = 1;
  while (backAlt > 1)
  {
    backAlt /= n;
    pot *= 2;
  }
  if (pot )
}

int main()
{
  while (scanf("%lld %lld", &altIni, &workers) && !(altIni == 0 && workers == 0))
  {
    n = 2;
    while ( (1 / (n + 1)) * 1 )
    {
      n ++;
    }

    printf("%lld %lld\n", gatos, pilha);
  }
  return(0);
}
